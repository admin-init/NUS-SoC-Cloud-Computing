apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ticket-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ticket-service
            port:
              number: 80
